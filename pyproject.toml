[build-system]
build-backend = "setuptools.build_meta"
requires = [
    "setuptools>=45",
    "wheel",
    "pymaat>=0.6.2",
    "eth_abi",
    "pysha3",
    "rlp",
    "crytic-compile",
    "solc-select"
]

[tool.black]
target-version = ['py37']
line-length = 80